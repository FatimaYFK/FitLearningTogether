<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dashboard">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fhcampus4"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fhcampus4"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dashboard">
        <activity android:name=".Yourprofil"></activity>
        <activity android:name=".EditProfile" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".All" />
        <activity android:name=".HTM" />
        <activity android:name=".Greenhouse" />
        <activity android:name=".Profile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>